// Optional Supabase availability flag
// When the Supabase integration is connected in Lovable, this file can be updated
// to expose a real client or a boolean flag toggled at build time.
// For now, we only export a flag to keep the app compiling without the client.

export const isSupabaseConnected = false;
